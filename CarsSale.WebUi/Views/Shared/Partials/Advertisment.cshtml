@model IEnumerable<CarsSale.DataAccess.DTO.Advertisement>

@if (Model?.Count() > 0)
{
    <div class="row">
        @foreach (var x in Model)
        {
            @Html.DisplayFor(m => x, "Advertisment")
            <hr>
        }
    </div>
}
